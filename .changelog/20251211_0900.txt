fix
Fix race condition in policy store cache invalidation
Resolves an issue where concurrent policy updates could cause stale cache entries to persist beyond their intended TTL, leading to incorrect authorization decisions.
